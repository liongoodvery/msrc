#include <iostream>
#include <vector>
#include <algorithm>
#include <iterator>
using namespace std;

vector<int> twoSum(vector<int> &nums, int target) {
    sort(begin(nums), end(nums));

    for(begin())
}

int main() {
    vector<int> v = {3,2,5,6};
    twoSum(v, 5);
    return 0;
}